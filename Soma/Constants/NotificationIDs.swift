import Foundation

// MARK: - Push Notification Identifiers
// All UNNotificationRequest identifiers in one place.

enum NotificationIDs {
    static let dailyRecovery   = "com.soma.daily-recovery"
    static let dailyInsights   = "com.soma.daily-insights"
    static let bedtimeReminder = "com.soma.bedtime-reminder"
    static let checkinReminder = "com.soma.checkin-reminder"
    static let weeklyNarrative = "com.soma.weekly-narrative"
}
